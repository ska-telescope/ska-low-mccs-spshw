defaults:
  logging_level_default: 5


platform:
  cluster:
    domain: cluster.local

# Use helm_values to set arbitrary helm values.
# These are passed through to the helm values file as-is.
# For example,
#
helm_values:
  # Null ParentTRL given by platform spec, currently the only place to do this.
  ska-low-mccs-daq:
    ska-tango-devices:
      labels:
        # This label is used by the make infrastructure
        # It was added here for to ensure that daq pods are waited for in the k8s-wait stage
        # on pipeline
        app: ska-low-mccs-spshw # This label is used by the make infrastructure (e.g. k8s-wait.)
      # deviceServerTypes:
      #   daq:
      #     image:
      #       registry: registry.gitlab.com/ska-telescope/mccs/ska-low-mccs-daq
      #       tag: 6.0.0-dev.cb82dcc01
  ska-tango-devices:
    devices:
      SpsStation:
        low-mccs/spsstation/ci-1:
          ParentTRL: ""
        low-mccs/spsstation/real-daq-1:
          ParentTRL: ""
  # image:
  #   registry: registry.gitlab.com/ska-telescope/mccs/ska-low-mccs-spshw # docker.io/library # registry.gitlab.com/ska-telescope/mccs/ska-low-mccs-spshw
  #   tag: 11.4.3-dev.c2a4b0594 # 11.4.3

